import type React from 'react'

import style from './ProjectsGrid.module.scss'

const ProjectsGrid: React.FC = () => {
  return <div className={style.gridContainer}></div>
}

export default ProjectsGrid
